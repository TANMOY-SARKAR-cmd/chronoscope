import { trpc } from "@/lib/trpc";
import { ArrowLeft, ShieldCheck, Trophy } from "lucide-react";
import { Link } from "wouter";

function format(value: number) { return Number.isFinite(value) ? value.toFixed(3) : "—"; }

export default function Leaderboard() {
  const leaderboard = trpc.chronomesh.publicStabilityLeaderboard.useQuery(undefined, { staleTime: 30_000, refetchInterval: 30_000 });
  return <div className="min-h-screen bg-[#090a0a] text-[#e4e4e7] cyber-grid"><header className="border-b border-[#a3e635]/20 bg-[#090a0a]/95"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-4"><Link href="/" className="numeric inline-flex items-center gap-2 text-xs text-[#a3e635]"><ArrowLeft className="h-4 w-4" />RETURN TO TIME MESH</Link><div className="numeric flex items-center gap-2 text-[10px] tracking-widest text-[#a3e635]"><Trophy className="h-4 w-4" />PUBLIC STABILITY INDEX</div></div></header><main className="mx-auto max-w-6xl px-5 py-10"><div className="grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><div><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center border border-[#a3e635]/50 bg-[#a3e635]/10"><Trophy className="h-5 w-5 text-[#a3e635]" /></div><div><h1 className="text-2xl font-bold tracking-[.12em]">CONSENTED STABILITY LEADERBOARD</h1><p className="numeric mt-1 text-[10px] tracking-wider text-[#71717a]">MEASURED SETUP SUMMARIES / NOT A HARDWARE CERTIFICATION</p></div></div><p className="mt-6 max-w-2xl text-sm leading-relaxed text-[#a1a1aa]">Every row was explicitly published by a signed-in operator. The table reveals only a self-chosen anonymous label, optional saved tags, and the latest aggregate timing estimate. It never exposes a room code, peer identifier, IP address, or private setup description.</p></div><div className="border border-[#a3e635]/25 bg-[#a3e635]/[.035] p-4"><div className="flex items-center gap-2 text-xs font-semibold text-[#a3e635]"><ShieldCheck className="h-4 w-4" />HOW SCORES WORK</div><p className="mt-2 text-xs leading-relaxed text-[#a1a1aa]">Stability rewards retained sample count and penalizes observed jitter. Offset and uncertainty remain visible, because a score alone does not establish absolute clock accuracy.</p></div></div><div className="mt-8 overflow-x-auto border border-[#a3e635]/20 bg-black/25"><table className="w-full min-w-[800px] text-left"><thead className="numeric bg-[#a3e635]/8 text-[10px] tracking-[.14em] text-[#a1a1aa]"><tr><th className="px-4 py-3">RANK</th><th className="px-4 py-3">ANONYMOUS SETUP</th><th className="px-4 py-3">STABILITY</th><th className="px-4 py-3">OFFSET ± UNCERTAINTY</th><th className="px-4 py-3">JITTER / SAMPLES</th><th className="px-4 py-3">LAST PUBLISHED</th></tr></thead><tbody>{leaderboard.data?.length ? leaderboard.data.map((entry, index) => <tr key={`${entry.setupLabel}-${entry.updatedAt.toISOString()}`} className="border-t border-[#a3e635]/10 text-xs"><td className="numeric px-4 py-3 text-[#71717a]">{String(index + 1).padStart(2, "0")}</td><td className="px-4 py-3"><div className="text-[#f4f4f5]">{entry.setupLabel}</div>{entry.hardwareTags.length > 0 && <div className="mt-1 flex flex-wrap gap-1">{entry.hardwareTags.map(tag => <span key={tag} className="numeric border border-[#a3e635]/25 px-1 text-[9px] text-[#a3e635]">{tag}</span>)}</div>}</td><td className="numeric px-4 py-3 text-[#a3e635]">{format(entry.stabilityScore)}/100</td><td className="numeric px-4 py-3 text-[#e4e4e7]">{entry.offsetMs >= 0 ? "+" : ""}{format(entry.offsetMs)} ms <span className="text-[#a3e635]">± {format(Math.abs(entry.uncertaintyMs))} ms</span></td><td className="numeric px-4 py-3 text-[#a1a1aa]">{format(entry.jitterMs)} ms / {entry.sampleCount}</td><td className="numeric px-4 py-3 text-[10px] text-[#71717a]">{entry.updatedAt.toLocaleString()}</td></tr>) : <tr><td colSpan={6} className="numeric px-4 py-14 text-center text-xs text-[#71717a]">NO CONSENTED SETUPS HAVE BEEN PUBLISHED YET.</td></tr>}</tbody></table></div></main></div>;
}
